import { LoginCard } from './login-card';

export default async function LoginPage() {
  return (
    <div className="flex items-center justify-center min-h-dvh p-6 md:p-12">
      <LoginCard />
    </div>
  );
}
